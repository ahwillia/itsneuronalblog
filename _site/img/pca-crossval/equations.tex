% approx %
$\approx$

% x %
$\mathbf{x}$

% bX %
$\mathbf{X}$

% bA %
$\mathbf{A}$

% bb %
$\mathbf{b}$

% bU %
$\mathbf{U}$

% bVt %
$\mathbf{V}^T$

% bY %
$\mathbf{Y}$

% lam %
$\lambda^{-1}$

% tens %
$\mathcal{T}$

% tens %
$\mathcal{T}$

% equation %
\[\mathbf{A}^T ( \mathbf{M} \circ (\mathbf{A} \mathbf{X})) = \mathbf{A}^T (\mathbf{M} \circ \mathbf{B})\]

% equals %
\[=\]

% slice %
\[\mathbf{A}^T \text{diag}(\mathbf{m}_j) \mathbf{A} \mathbf{x} = \mathbf{A}^T \text{diag}(\mathbf{m}_j) \mathbf{b}_j \]

% xj %
$\mathbf{x}_j$

% rtr %
\[r \times r\]

% rt1 %
\[r \times 1\]

% xi %
\[\mathbf{x}_i\]

% bi %
\[\mathbf{b}_i\]

% bb_matrix %
\[\mathbf{B}\]

% Y_1_1 %
\[\mathbf{Y}_{(1,1)}\]

% Y_1_2 %
\[\mathbf{Y}_{(1,2)}\]

% Y_2_2 %
\[\mathbf{Y}_{(2,2)}\]

% Y_2_1 %
\[\mathbf{Y}_{(2,1)}\]
